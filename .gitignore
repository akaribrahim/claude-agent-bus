# Generated by the installer: which entry point this machine uses (the bash fast
# path or the Python fallback) and the interpreter path baked into it are local
# facts, not something to share between machines.
hooks/hooks.json

__pycache__/
*.pyc
.DS_Store
